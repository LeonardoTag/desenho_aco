<Window x:Class="CapitalAco.DrawingMacro.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:CapitalAco.DrawingMacro.App.Views"
        Title="Capital Aço Detalhador" Height="750" Width="1300"
        MinHeight="600" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        Icon="/Assets/logo.ico"
        Background="#F4F5F9">
    <Window.InputBindings>
        <KeyBinding Gesture="Ctrl+1" Command="{Binding IrParaAbaCommand}" CommandParameter="0"/>
        <KeyBinding Gesture="Ctrl+2" Command="{Binding IrParaAbaCommand}" CommandParameter="1"/>
        <KeyBinding Gesture="Ctrl+3" Command="{Binding IrParaAbaCommand}" CommandParameter="2"/>
        <KeyBinding Gesture="Ctrl+4" Command="{Binding IrParaAbaCommand}" CommandParameter="3"/>
    </Window.InputBindings>
    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
            <Image Source="/Assets/logo.png" Height="30" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="CAPITAL AÇO" Foreground="#12153F" FontSize="18" FontWeight="Bold" FontFamily="Segoe UI"/>
                <TextBlock Text="Sistema de Detalhamento de Dobras e Geração de Pedidos" Foreground="#5B6472" FontSize="11" Margin="0,2,0,0" FontFamily="Segoe UI"/>
            </StackPanel>
        </StackPanel>

        <TabControl Grid.Row="1" Background="#ECEEF3" BorderBrush="#FFFFFF" SelectedIndex="{Binding SelectedTabIndex}">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="14,8"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Bd" Background="#FFFFFF" BorderBrush="#E2E5ED" BorderThickness="1,1,1,0" CornerRadius="6,6,0,0" Margin="2,0,2,0" TextElement.Foreground="#1F2430">
                                    <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#12153F"/>
                                        <Setter TargetName="Bd" Property="TextElement.Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Header="Editor de Peça">
                <views:EditorPecaView DataContext="{Binding Editor}"/>
            </TabItem>
            <TabItem Header="Biblioteca de Peças">
                <views:BibliotecaView DataContext="{Binding Biblioteca}"/>
            </TabItem>
            <TabItem Header="Ordem de Produção">
                <views:PedidoView DataContext="{Binding Pedido}"/>
            </TabItem>
            <TabItem Header="Configurações">
                <views:ConfiguracaoView DataContext="{Binding Configuracao}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
