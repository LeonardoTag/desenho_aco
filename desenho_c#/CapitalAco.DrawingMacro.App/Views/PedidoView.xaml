<UserControl x:Class="CapitalAco.DrawingMacro.App.Views.PedidoView"
             xmlns="http://schemas.microsoft.com/winfx/2000/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2000/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200"
             Background="#2C3E50"
             Foreground="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Grade de Itens -->
        <Border Grid.Row="0" Background="#34495E" CornerRadius="8" Padding="15" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="CARRINHO DA ORDEM DE PRODUÇÃO" FontSize="16" FontWeight="Bold" Foreground="#27AE60" Margin="0,0,0,10"/>
                
                <DataGrid Grid.Row="1" ItemsSource="{Binding Itens}" AutoGenerateColumns="False" CanUserAddRows="False" Background="#2C3E50" Foreground="White" RowBackground="#34495E" AlternatingRowBackground="#2C3E50" BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#5F6A6A" HeadersVisibility="Column">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#1E2022"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Nome da Peça" Binding="{Binding NomePeca}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Chapa" Binding="{Binding ChapaCodigo}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Comprimento" Binding="{Binding Comprimento, StringFormat='{}{0} mm'}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Qtd" Binding="{Binding Quantidade}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Observação do Item" Binding="{Binding Observacao}" Width="2*" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="Ações" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remover" Command="{Binding DataContext.RemoverItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="2" Background="#C0392B" Foreground="White" BorderThickness="0" Height="22" FontWeight="SemiBold" FontSize="11"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Observações e Ações Globais -->
        <Border Grid.Row="1" Background="#34495E" CornerRadius="8" Padding="15" Margin="5,10,5,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="320"/>
                </Grid.ColumnDefinitions>

                <!-- Observação Geral -->
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="OBSERVAÇÃO GERAL (IMPRESSA NO RELATÓRIO):" FontSize="12" FontWeight="Bold" Foreground="#BDC3C7" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Observacao, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="70" Background="#2C3E50" Foreground="White" BorderBrush="#7F8C8D" VerticalContentAlignment="Top" Padding="5"/>
                </StackPanel>

                <!-- Botões de Ação -->
                <Grid Grid.Column="1" VerticalAlignment="Bottom">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" Content="Limpar Pedido" Command="{Binding LimparPedidoCommand}" Margin="0,0,0,8" Background="#D35400" Foreground="White" BorderThickness="0" Height="32" FontSize="13" FontWeight="SemiBold"/>
                    <Button Grid.Row="1" Content="Gerar Ordem de Produção (PDF)" Command="{Binding GerarPdfCommand}" Background="#27AE60" Foreground="White" BorderThickness="0" Height="40" FontSize="14" FontWeight="Bold"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
